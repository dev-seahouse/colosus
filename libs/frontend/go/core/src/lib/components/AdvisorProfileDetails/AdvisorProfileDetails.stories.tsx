import type { Meta } from '@storybook/react';
import { AdvisorProfileDetails } from './AdvisorProfileDetails';

const Story: Meta<typeof AdvisorProfileDetails> = {
  component: AdvisorProfileDetails,
  title: 'Core/components/AdvisorProfileDetails',
};
export default Story;

export const Primary = {
  args: {},
};
