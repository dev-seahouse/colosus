import type { Meta } from '@storybook/react';
import { AppointmentScheduledPage } from './AppointmentScheduledPage';

const Story: Meta<typeof AppointmentScheduledPage> = {
  component: AppointmentScheduledPage,
  title: 'Engagement/pages/AppointmentScheduledPage',
};
export default Story;

export const Primary = {
  args: {},
};
