import type { Meta } from '@storybook/react';
import { ProductRecommendationTabs } from './ProductRecommendationTabs';

const Story: Meta<typeof ProductRecommendationTabs> = {
  component: ProductRecommendationTabs,
  title: 'goal-settings/components/ProductRecommendationTabs',
};
export default Story;

export const Primary = {
  args: {},
};
