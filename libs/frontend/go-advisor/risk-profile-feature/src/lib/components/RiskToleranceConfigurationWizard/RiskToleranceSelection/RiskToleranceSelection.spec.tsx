import { render } from '@bambu/react-test-utils';

import RiskToleranceSelection from './RiskToleranceSelection';

// TODO: Implement unit test
describe('RiskToleranceSelection', () => {
  it.skip('should render successfully', () => {
    const { baseElement } = render(<RiskToleranceSelection />);
    expect(baseElement).toBeTruthy();
  });
});
