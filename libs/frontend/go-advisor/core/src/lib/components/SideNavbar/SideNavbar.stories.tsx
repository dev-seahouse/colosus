import type { Meta } from '@storybook/react';
import { SideNavbar } from './SideNavbar';

const Story: Meta<typeof SideNavbar> = {
  component: SideNavbar,
  title: 'Auth/components/SideNavbar',
};
export default Story;

export const Primary = {
  args: {},
};
