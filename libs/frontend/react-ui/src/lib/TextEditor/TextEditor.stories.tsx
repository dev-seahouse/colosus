import type { Meta } from '@storybook/react';
import { TextEditor } from './TextEditor';

const Story: Meta<typeof TextEditor> = {
  component: TextEditor,
  title: 'TextEditor',
};
export default Story;

export const Primary = {
  args: {},
};
