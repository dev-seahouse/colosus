import type { Meta } from '@storybook/react';
import { PageNotFound } from './PageNotFound';

const Story: Meta<typeof PageNotFound> = {
  component: PageNotFound,
  title: 'PageNotFound',
};
export default Story;

export const Primary = {
  args: {},
};
